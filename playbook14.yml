---
- name: Install multiple s/w apps
  hosts: all
  become: yes
  tasks:
    - name: Install s/w's
      apt:
        name: "{{ item }}"
        state: present
        update_cache: yes
      loop:
        - git
        - maven
        - openjdk-17-jdk
...

