---
- name: Implementing host scope variables for a single server
  hosts: 172.31.21.159
  tasks:
    - name: user creation
      user:
        name: "{{a}}"
        password: "{{b}}"
        uid: "{{c}}"
        home: "{{d}}"
        comment: "{{e}}"
        shell: "{{f}}"
... 

